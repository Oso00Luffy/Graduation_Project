import 'package:flutter/material.dart';

class FileSenderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('File Sender'),
      ),
      body: Center(
        child: Text('File Sender Functionality Here'),
      ),
    );
  }
}